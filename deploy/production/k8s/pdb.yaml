# Phase 15 — voluntary-disruption budget: at least one API pod stays up during
# node maintenance / rolling updates.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: fleetflow-api
  namespace: fleetflow
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: fleetflow-api