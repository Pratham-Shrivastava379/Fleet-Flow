# Phase 15 — dashboard ClusterIP service (ingress-terminated).
apiVersion: v1
kind: Service
metadata:
  name: fleetflow-dashboard
  namespace: fleetflow
spec:
  selector:
    app: fleetflow-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP