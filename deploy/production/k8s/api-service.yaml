# Phase 15 — internal ClusterIP service for the API (REST + WS both on :3000).
apiVersion: v1
kind: Service
metadata:
  name: fleetflow-api
  namespace: fleetflow
spec:
  selector:
    app: fleetflow-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP