node_modules/
backend/dev.db
*.log
*.out
android/.gradle/
android/build/
android/app/build/
android/fcm-plugin/build/
android/fcm-plugin/.gradle/
android/local.properties
# §6: Firebase credentials never enter the repo (dropped in manually by the
# developer when FCM should activate; builds work without it).
android/app/google-services.json
android/.idea/
.idea/
*.db-journal
.env
.env.*
!.env.example
!**/.env.example
!**/.env.*.example
.DS_Store
Thumbs.db
coverage/
# Android release signing artifacts — the keystore NEVER enters the repo (§13.4)
android/*.jks
android/*.keystore
android/keystore.properties
android/app/*.jks
# Deploy-script runtime state (previous image tag for rollback)
deploy/staging/.state/
